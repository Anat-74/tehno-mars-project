<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # Передача всех остальных запросов на Nuxt приложение
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ / [QSA,L]
</IfModule>